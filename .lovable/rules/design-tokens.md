# Design Tokens

Token reference for **Gentle Library**. Use utility classes and CSS variables — never raw values.

## Colors

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--color-canvas` |
| `--color-canvas-deep` |
| `--color-paper` |
| `--color-paper-highlight` |
| `--color-ink` |
| `--color-ink-muted` |
| `--color-wine` |
| `--color-wine-deep` |
| `--color-wine-highlight` |
| `--color-gold` |
| `--color-sage` |

## Typography

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--color-line` |
| `--font-display` |
| `--font-script` |
| `--font-body` |

## Spacing

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--space-1` |
| `--space-2` |
| `--space-3` |
| `--space-4` |
| `--space-6` |
| `--space-8` |
| `--space-12` |
| `--space-16` |

## Border Radius

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--radius-sm` |
| `--radius-md` |
| `--radius-round` |

## Shadows

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--color-paper-shadow` |
| `--shadow-paper` |
| `--shadow-seal` |

## Other

Reference via `var(--name)` in inline styles or CSS.

| CSS variable |
|---|
| `--duration-fast` |
| `--duration-page` |

